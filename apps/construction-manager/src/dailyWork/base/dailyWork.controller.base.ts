/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { DailyWorkService } from "../dailyWork.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { DailyWorkCreateInput } from "./DailyWorkCreateInput";
import { DailyWorkWhereInput } from "./DailyWorkWhereInput";
import { DailyWorkWhereUniqueInput } from "./DailyWorkWhereUniqueInput";
import { DailyWorkFindManyArgs } from "./DailyWorkFindManyArgs";
import { DailyWorkUpdateInput } from "./DailyWorkUpdateInput";
import { DailyWork } from "./DailyWork";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class DailyWorkControllerBase {
  constructor(
    protected readonly service: DailyWorkService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: DailyWork })
  @nestAccessControl.UseRoles({
    resource: "DailyWork",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(@common.Body() data: DailyWorkCreateInput): Promise<DailyWork> {
    return await this.service.create({
      data: data,
      select: {
        constructionId: true,
        createdAt: true,
        employeeId: true,
        id: true,
        updatedAt: true,
        workDate: true,
        workHours: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [DailyWork] })
  @ApiNestedQuery(DailyWorkFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "DailyWork",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<DailyWork[]> {
    const args = plainToClass(DailyWorkFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        constructionId: true,
        createdAt: true,
        employeeId: true,
        id: true,
        updatedAt: true,
        workDate: true,
        workHours: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: DailyWork })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DailyWork",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: DailyWorkWhereUniqueInput
  ): Promise<DailyWork | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        constructionId: true,
        createdAt: true,
        employeeId: true,
        id: true,
        updatedAt: true,
        workDate: true,
        workHours: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: DailyWork })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DailyWork",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: DailyWorkWhereUniqueInput,
    @common.Body() data: DailyWorkUpdateInput
  ): Promise<DailyWork | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          constructionId: true,
          createdAt: true,
          employeeId: true,
          id: true,
          updatedAt: true,
          workDate: true,
          workHours: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: DailyWork })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "DailyWork",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: DailyWorkWhereUniqueInput
  ): Promise<DailyWork | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          constructionId: true,
          createdAt: true,
          employeeId: true,
          id: true,
          updatedAt: true,
          workDate: true,
          workHours: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
